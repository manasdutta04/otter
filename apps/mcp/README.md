# veridexs MCP bridge

Run `python server.py` as a stdio JSON-RPC process. It exposes the repository intelligence tool and forwards requests to the existing API using `VERIDEXS_API_URL`. This keeps MCP consumers on the same product contract as the dashboard and CLI.
