# Deprecated alias — use compose.platform.yml (self-host) or compose.dev.yml (contributors).
# Kept so older docs/scripts that pass compose.yml still resolve to the platform stack.

include:
  - path: compose.platform.yml
