name: Publish @otter-engg/cli (tag)

# Tag publishes are handled by cli-publish.yml (npm allows one Trusted Publisher workflow).

on:
  push:
    tags:
      - "cli-v*"

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Tag publishes run via cli-publish.yml."
